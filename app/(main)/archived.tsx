import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
export default function ArchivedScreen() {
  return (
    <SafeAreaView>
      <Text className="text-2xl">Archived Notes</Text>
    </SafeAreaView>
  );
}
